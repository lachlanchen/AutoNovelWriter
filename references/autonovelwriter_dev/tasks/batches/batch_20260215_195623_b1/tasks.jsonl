{"id": "T007_pipeline_script_v2_indent", "title": "Pipeline script v2: indent + validate", "notes": "Extend canonical `pipeline.script` format to support nesting via indentation (e.g. `LOOP <n>` with indented `STEP/DISABLED`). Backend must round-trip script <-> structured JSON <-> script for supported constructs, and return parse warnings/errors explicitly (no silent ignoring). Keep v1 `STEP/DISABLED <type>` fully supported.", "acceptance": ["POST /api/pipeline with v1 script still returns ok and preserves behavior", "POST /api/pipeline with v2 indented script returns ok and derived JSON includes nesting", "GET /api/pipeline returns a canonical script that round-trips back to the same derived JSON", "Invalid indent/unknown verbs produce 400 or warnings with line numbers"], "tags": ["backend", "pipeline", "script", "validation"]}
{"id": "T008_blocks_indent_outdent_ui", "title": "Blocks UI: indent/outdent + nested render", "notes": "Update PWA to render nested pipeline blocks from structured JSON (from T007). Add per-block indent/outdent controls (buttons + keyboard shortcuts) and keep the script textarea as canonical (edits sync both ways). Keep drag reorder within same nesting level initially.", "acceptance": ["Nested blocks render with clear indentation and grouping", "Indent/outdent updates script textarea and derived blocks immediately", "Save persists script+JSON; reload restores nesting", "Drag reorder works within a nesting level without corrupting structure"], "tags": ["pwa", "pipeline", "ui", "script"]}
{"id": "T009_storage_layout_projects_materials", "title": "Storage layout: projects + materials index", "notes": "Define a standardized runtime storage layout under `autonovelwriter/runtime/` for projects, materials, interactions, outputs, logs, tasks, and resume state. Add backend indexing endpoint(s) and a minimal PWA materials browser (read-only) with an active project selector.", "acceptance": ["Backend exposes GET /api/projects and GET /api/materials/index (active project + file listing)", "PWA shows active project and materials list (read-only)", "Dropping a file into the materials folder appears in the index without restart"], "tags": ["backend", "pwa", "storage", "materials"]}
{"id": "T010_novel_settings_separate_from_ui_lang", "title": "Novel settings: language + UX options", "notes": "Add novel-writing settings that are explicitly separate from UI language: novel language, tone, target length, POV, etc. Persist via `/api/settings` under a `novel` namespace and expose in PWA Settings UI (without changing UI labels).", "acceptance": ["Settings persist under settings.novel.* and survive backend restart", "Changing UI language does not change novel language setting and vice versa", "PWA displays and edits novel settings via /api/settings"], "tags": ["pwa", "backend", "settings", "novel"]}
{"id": "T011_runner_writes_outputs_stub", "title": "Runner: write drafts/revisions to outputs", "notes": "Make the in-app runner actually write files into the standardized outputs folders for the active project. Implement at least one runnable pipeline action that produces a draft file (placeholder content allowed), emits WS events with file paths, and updates task status persistently.", "acceptance": ["Running a pipeline that includes the write action produces a new output file on disk", "WS emits an event/log with the created file path", "Task status persists across backend restart and reflects the write step completion"], "tags": ["backend", "runner", "outputs", "pipeline"]}
