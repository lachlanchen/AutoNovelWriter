{"id": "T012_runner_use_pipeline_v2", "title": "Runner: execute v2 pipeline script/AST", "notes": "Switch runner block selection to use the canonical pipeline script v2 parser/AST (indent + LOOP) so execution order matches what the PWA edits/displays. Initially ignore LOOP repeat counts (treat LOOP as grouping) or implement repeat deterministically; but runner must not use legacy flat parsing that can diverge from UI.", "acceptance": ["Runner uses v2 pipeline parsing (same as /api/pipeline) to derive enabled steps", "A pipeline with nested LOOPs results in a deterministic flattened execution order matching the AST preorder", "No TCP-bind verification needed: py_compile passes and a small unit-style function test can flatten AST to steps"], "tags": ["backend", "runner", "pipeline", "resumability"]}
{"id": "T013_outputs_index_api", "title": "Outputs index API (active project)", "notes": "Add a small API to list output files for the active project (path, mtime, size, kind) similar to materials index, and cap results. This enables PWA observability without requiring filesystem access.", "acceptance": ["GET /api/outputs/index returns ok=true with files[] under runtime/projects/<active>/outputs", "Response includes project_id and outputs_root", "Indexing is capped (e.g. 5000 entries) and sorted for stability"], "tags": ["backend", "outputs", "projects", "api"]}
{"id": "T014_pwa_outputs_panel", "title": "PWA: outputs panel + WS hook", "notes": "Add a minimal Outputs panel in the PWA that lists active project outputs via /api/outputs/index and reacts to WS output_created events (append a toast/log and refresh index with debounce). Keep light theme and i18n keys for all 11 UI languages.", "acceptance": ["PWA shows outputs list for active project and updates after output_created WS event", "New UI strings are localized for en, zh-Hans, zh-Hant, ja, ko, vi, ar, fr, es, ru, de", "node --check passes; no server start required in sandbox"], "tags": ["pwa", "outputs", "ws", "i18n"]}
{"id": "T015_pipeline_script_round_foreach_parse", "title": "Pipeline script: ROUND/FOREACH_TASK parse+render", "notes": "Extend pipeline script format to support the default Scratch-like layout verbs ROUND <n_rounds> and FOREACH_TASK as structured loop constructs (canonical script remains source of truth). Map ROUND to a loop node with repeat=n_rounds, and FOREACH_TASK to a loop-like node with repeat='foreach_task' or a dedicated kind; render back canonically. Do not conflate with driver stages.", "acceptance": ["Parser accepts ROUND <n> and FOREACH_TASK with 2-space indentation and produces a nested AST", "Renderer round-trips AST back to canonical script with ROUND/FOREACH_TASK preserved", "Validation rejects bad indentation and unknown verbs with structured errors/warnings"], "tags": ["backend", "pipeline", "parser", "ui-blocks"]}
{"id": "T016_pwa_loop_repeat_editor", "title": "Blocks UI: edit loop repeat counts", "notes": "Add loop repeat count editing in the blocks UI for LOOP/ROUND blocks (inline input or modal), including validation (int >=1) and updating canonical script. Ensure keyboard accessibility and i18n for labels/errors.", "acceptance": ["User can change a loop repeat count and the canonical script updates immediately", "Invalid repeats are prevented or validated with user-visible feedback", "All new strings localized for 11 UI languages"], "tags": ["pwa", "pipeline", "blocks", "i18n"]}
{"id": "T017_meta_tasks_generate_stub", "title": "Runner: meta_tasks_generate writes task batch", "notes": "Implement a stub pipeline action meta_tasks_generate that creates a new tasks batch folder under runtime (standardized layout) and writes a JSONL task list derived from current materials + recent interactions (placeholder content OK). Must be resumable and observable via WS/log, and must not conflict with driver task queue semantics.", "acceptance": ["Running a pipeline containing meta_tasks_generate creates a new batch folder and at least one JSONL file on disk", "WS/log includes the created batch path and task count", "Task status persists across restart and records meta_tasks_generate completion with output paths"], "tags": ["backend", "runner", "tasks", "storage"]}
